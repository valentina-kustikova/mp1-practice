#include <stdio.h>
#include <stdlib.h>
#include <time.h>
void main () 
{
	srand((unsigned int)time(0));
	int a[10];
	int b[10];
	int n, B, K, i;
	printf ("input n /n");
	scanf ("%d", &n);
	for (i = 0; i < n+1; i++)
	{
		a[i] = rand() % 10;
	}
	while (K != n)
	{
		printf ("input b");
		for (i =0; i < n+1; i++)
		{
			
		}
	}
}
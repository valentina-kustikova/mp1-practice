#include "includes.hpp"

int main()
{
	TodoList::app instance;

	// Comment line below if you don't want to use a built-in interface
	instance.start();

	// Uncomment line below if you want to use an example interface instead of the built-in one
	// #include <interface.inl>
}
#ifndef __GLOBAL_NAMES
#define __GLOBAL_NAMES

#define N 1000
#define sizedb 15

extern char* given_codes[sizedb];
extern char* name[sizedb];
extern int price[sizedb];
extern float discount[sizedb];
extern int cnt[sizedb];

#endif
